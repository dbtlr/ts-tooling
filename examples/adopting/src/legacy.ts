// A deliberately un-tidy module standing in for unmigrated code: under lenient
// lint these are warnings, not errors, so `vp check` still passes.
export const add = (a: number, b: number) => {
  return a + b;
};
